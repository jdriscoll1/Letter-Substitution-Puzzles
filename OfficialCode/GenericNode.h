/*The purpose of this is to create a structure that contains a word, and a void piece of data, so that a word can be stored with anything*/

struct GenericNode {
	//This is a data with which can store anything
	void* data; 
	//This is the word to be accompagnied with the anything
	char* word; 
};

//These are just methods to quickly fill it with any data
//The only thing it needs right now is an AVL Tree
struct GenericNode* Fill_TreeSet(){
	
	
}
